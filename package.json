{
  "name": "dawnandbendotcom",
  "version": "1.0.0",
  "description": "Our page",
  "scripts": {
    "clean": "rimraf ./dist/",
    "build": "eleventy",
    "build-prod": "cross-env NODE_ENV=production npm run clean && npm run build",
    "watch": "eleventy --serve --port=8080",
    "start": "netlify dev"
  },
  "keywords": [
    "11ty",
    "eleventy"
  ],
  "dependencies": {
    "@11ty/eleventy": "^1.0.0",
    "cross-env": "^7.0.3",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2"
  }
}
